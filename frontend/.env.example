# Set this to your deployed backend's URL (no trailing slash), e.g.:
# VITE_API_URL=https://api.yourdomain.com
# Leave unset for local dev -- the Vite proxy in vite.config.js handles /api locally.
VITE_API_URL=